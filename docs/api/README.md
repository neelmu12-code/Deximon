# API contracts

OpenAPI 3.1 specs, one file per service. Specs are the source of truth: any PR that changes an endpoint must update the matching spec.

| Service | File              | Status   |
|---------|-------------------|----------|
| Auth / Profile (S1)        | `auth.yaml`     | stub     |
| Collection / Marketplace (S2) | `collection.yaml` | stub |
| Chat / Notification (S3)   | `chat.yaml`     | stub     |
| Scanner (S4)               | `scanner.yaml`  | stub     |

Recommended VS Code extension: **42Crunch.vscode-openapi** — gives schema-aware editing and live preview.
