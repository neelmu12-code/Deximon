openapi: 3.1.0
info:
  title: Deximon — Auth & Profile API
  version: 0.0.1
  description: Service 1 (S1). Owns identity, accounts, public profiles, reviews.
servers:
  - url: http://localhost:8000
    description: Local
paths:
  /healthz:
    get:
      summary: Liveness probe
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema: { type: object, properties: { status: { type: string } } }
components:
  schemas: {}
